server:
  port: 9090

# Zeebe 网关连接地址，不配置 Worker 不会注册
zeebe:
  gateway:
    cluster:
      initialContactPoints: localhost:26500

# 日志级别，方便查看 Worker 打印信息
logging:
  level:
    com.pizza.worker: DEBUG
    io.camunda.zeebe: INFO