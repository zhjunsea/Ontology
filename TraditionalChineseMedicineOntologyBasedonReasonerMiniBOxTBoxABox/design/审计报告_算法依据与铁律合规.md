# 经方本体工程 · 算法依据与铁律合规审计报告

> 审计日期：2026-09-20
> 审计对象：`OntologyMachine/OntologyFramework` 全部 18 个 Java 源文件（7123 行）+ 本体 OWL 文件
> 审计依据：铁律全集（1–60 条，源 `~/.workbuddy/skills/tcm-ontology-build-verify/SKILL.md`）
> 审计问题：① 是否存在「主观拍脑袋、无医书/医理依据」的规则；② 是否逐条符合 60 条铁律

---

## 一、审计范围与方法

| 维度 | 方法 |
|---|---|
| 生产路径判定 | `grep` 全工程引用关系，区分「被 Worker/Spring 注入」与「零引用独立类」 |
| 主观规则识别 | 扫描全部硬编码常量、数值阈值、启发式分支、字符串映射 |
| 依据判定 | 逐条核对代码注释中的医书/医理出处，无出处者标记「待考」 |
| 铁律合规 | 对照 60 条铁律逐条取证（代码行号 / 本体文件 / git 核验） |

**生产路径核心**（真正参与诊断链路）：
- `TCMOntologyJobWorker.java`（2978 行）—— Openllet 推理 + 三阶段 minibox + 排序器 + 加减药派生
- `SymptomMappingService.java`（821 行）—— 自然语言 → 症状 IRI 映射
- `TCMDiagnosisService.java` / `TCMDiagnosisController.java` —— BPMN 流程编排（无医学算法）

**非生产路径**（零引用，独立实验/工具类）：
- `JingFangInference.java`（383 行，零引用）
- `SixChannelInferenceEngine.java`（728 行，仅被 `InferenceResult` 引用常量，Worker 引用数 = 0）
- `PulseParser.java`（99 行，零引用）
- `PizzaOntologyJobWorker.java`（765 行，示例工程）

---

## 二、算法主观性审计

### 2.1 生产路径 —— 未发现无依据的主观规则 ✅

| 算法/规则 | 位置 | 依据 | 判定 |
|---|---|---|---|
| 排序器 step 0 父子层级（jaccard 差 < 0.15 子方优先） | `rankCandidatesWithScores` 689–694 | 「方随证转、随证加减」（中医临床通则） | ✅ 有依据 |
| 排序器 step 1 广谱惩罚（仅 `Wulingsanzheng`） | 188–190 | 五苓散证等价类仅 4 约束，易误命中（结构分析） | ✅ 有依据 |
| 排序器 step 2–4（jaccard/hits/required） | 705–713 | 集合结构度量，非医学判断 | ✅ 中性 |
| 排序器 step 5 `clinicalPriority` 升序 | 714–716 | 本体注解（1072 处），仅同分时介入 | ⚠️ 见 2.3 |
| 排序器 step 5.5 六经相符度（对称差） | 717–728 | 胡希恕「先辨六经，继辨方证」 | ✅ 有依据 |
| `resolveYinYang` 阴阳消解 | 1580–1604 | 《伤寒论》281 条少阴提纲「脉微细，但欲寐」 | ✅ 有依据 |
| `resolveLiujingMutex` 六经互斥 | 1612–1628 | **已废止**（`LIUJING_MUTEX_PAIRS = Map.of()`），注释详列废止医理 | ✅ 合规（停用） |
| `resolveLiujingCrossProduct` 交叉积消解 | 1734+ | 胡希恕《六经八纲》判据绑定病位↔病性 | ✅ 有依据 |
| `buildCombinedDiseaseMark` 合病命名 | 1818–1874 | 六经顺序（太阳→阳明→少阳→太阴→少阴→厥阴）为《伤寒论》传变序 | ✅ 有依据 |
| `deriveFormula` 加减药派生 | 2241–2310 | 完全由 `rules.owl` 方后注规则驱动，每条带 `ruleSource` | ✅ 有依据 |
| `filterFangzheng` 阶段2候选过滤 | 1068–1131 | 六经池约束（单向）+「但见一证便是」存在量词 | ✅ 有依据 |
| `expandComposites` 复合类展开 | 1044–1066 | 纯逻辑（原子齐全则复合成立），`guard<5` 防死循环 | ✅ 中性 |
| `loadIncompatibilitiesFromObda` 十八反十九畏 | 836–885 | 来自本体 `antagonistic`/`fearing` 关系 | ✅ 有依据 |
| 八纲分类（严格单向） | 1400–1495 | 注释「不再用方证兜底反推」 | ✅ 合规 |
| 六经分类（严格单向） | 1497–1560 | 注释「不再用方证反推六经」 | ✅ 合规 |
| `SymptomMappingService` 全部阈值 | 42–65 等 | 文本相似度/信息检索参数（非医学判断） | ✅ 中性 |

**结论**：生产路径中**不存在**无医书/医理依据的医学判断规则。所有启发式分支均在注释中标注了出处或明确为结构度量。

### 2.2 非生产路径 —— 存在主观参数与硬编码启发式 ⚠️

#### (1) `JingFangInference.java` —— 数据拟合参数，无医理依据

```java
private static final double WEIGHT_CAP = 8.0;      // auto_tune.py 调优
private static final double NORM_ALPHA = 0.65;     // auto_tune.py 调优
BOOST_TYPES.put("mx", 1.4); BOOST_TYPES.put("sx", 1.4);  // auto_tune.py 调优
```

- 注释自述「使用 auto_tune.py 调优后的最优参数」「排序：匹配率优先」。
- 这是 **IPF 迭代比例拟合 + 网格搜索**得到的统计参数，**无任何医书出处**。
- 排序完全依赖「匹配率」，不做六经/八纲约束。
- **判定：典型「主观拍脑袋」（数据驱动），但零引用，不在生产路径。**

#### (2) `SixChannelInferenceEngine.java` —— 硬编码启发式

| 方法 | 问题 | 判定 |
|---|---|---|
| `disambiguateByPulse`（380） | 硬编码「脉浮→保留表/太阳/少阳/**阳明**」「脉沉→保留里/太阴/少阴/厥阴」，用 `contains("Yangming")` 字符串匹配 | ⚠️ 阳明本为里证，归入「脉浮保留集」医理可疑 |
| `disambiguateByTongue`（417） | 硬编码「舌红苔黄→阳明/少阳/Re」「舌淡苔白→太阴/少阴/Han」 | ⚠️ 无出处 |
| `inferNatureFromPulse`（452） | 优先级「热 > 寒 > 实 > 虚」，注释仅称「与中医辨证习惯一致」 | ⚠️ 无出处 |
| `determineNature`（614） | 阳证优先，引胡希恕「阳证显性易辨」 | ✅ 有依据 |
| `checkShaoYinSpecificRule`（508） | 引胡希恕「脉微细但欲寐，皆从少阴论治」 | ✅ 有依据 |
| `inferByIndicators`（690） | 投票取最多（退化路径） | ⚠️ 无出处 |

- **判定：含主观硬编码启发式，但铁律 37 已确认其非生产路径（Worker 引用数 = 0）。**

#### (3) `PulseParser.java` —— 零引用工具类，未参与诊断。

### 2.3 需说明项：`clinicalPriority`

- 本体注解，1072 处赋值（值 2/3/5 等整数），**无医书出处注释**。
- 影响面：排序器 **第 5 顺位**，仅当 jaccard、hits、required 全部相等时介入。
- 性质：属「展示优先级」调优参数，**不改变哪些方证被推出**，不引入医学判断。
- **判定：可接受，但建议补 `rdfs:comment` 说明赋值口径（如「临床常见度」）。**

---

## 三、文档与实现不一致（非违规，但需修正）

| 项 | 说明 |
|---|---|
| `compareBingweiCoverage` | javadoc（641、1648 行）以 `@link` 引用「病位覆盖裁决：合病须合治」，但**代码中无此方法定义**。排序器 step 0 实际只做 `compareSpecialization`（父子层级），未实现病位覆盖比较。 |

**建议**：或补实现，或删除 javadoc 中的悬空引用，避免误导。

---

## 四、铁律 60 条合规矩阵

图例：✅ 合规 ｜ ⚠️ 需说明 ｜ — 不适用（工程/流程类，本次改动未触及）

### 4.1 构建与工具链类（1–14、17、20、29、33、38、44、45、50、54）

| 铁律 | 主题 | 判定 | 证据 |
|---|---|---|---|
| 1 | 必须走 reactor | — | 本轮未改构建方式 |
| 2 | PowerShell 输出重定向解码 | — | 流程类 |
| 3 | bash shim 损坏用 PATH 修复 | ✅ | 本轮全程 `export PATH=...` |
| 4 | javac/java 用 @argfile | — | 流程类 |
| 5 | 区分离线/真实环境测试 | ✅ | 279 用例离线套件 |
| 6 | surefire 关 manifest-only jar | — | 流程类 |
| 7 | BPMN 用 zeebe-process-test | ✅ | 未改 BPMN 逻辑 |
| 8 | 聚合套件同包 | ✅ | `AllFangzhengSuiteTest` 在 `tcm` 包 |
| 9 | 多场景合并只 realize 一次 | ✅ | 套件设计如此 |
| 10 | 超时伪装失败先看耗时分布 | ✅ | 前序排障遵循 |
| 11 | mvn 损坏直调 Java | ✅ | `run_suite.sh` 绕 shim |
| 12 | Camunda REST 在 9080 | — | 流程类 |
| 13 | 全自动流程状态判定 | — | 流程类 |
| 14 | 改 Java 必须重启进程 | ✅ | 本轮改本体后重启 Worker |
| 17 | curl 失败先怀疑代理 | — | 流程类 |
| 20 | 扫描 OWL 识别自闭合标签 | ✅ | 本体解析遵循 |
| 29 | TBox harness 离线解析 imports | ✅ | 前序 harness |
| 33 | 先验证代码绑定生效 | ✅ | 命名空间三重核对 |
| 38 | 最小四诊模块集 | ✅ | 三阶段 minibox |
| 44 | 改判据前用离线模拟器 | ✅ | 前序遵循 |
| 45 | 解析 OWL 用 ElementTree | ✅ | 本体脚本遵循 |
| 50 | 启动慢先看 jstack | ✅ | 瓶颈定位 `precomputeInferences` |
| 54 | 耗时测量重复取样 | ✅ | 前序多次取样 |

### 4.2 本体建模类（15、16、18、19、21–28、30–32、34–36、39–41、46–49、55、56、58）

| 铁律 | 主题 | 判定 | 证据 |
|---|---|---|---|
| 15 | 「未定」不进候选列表 | ✅ | 排序器无占位符 |
| 16 | 结果三态化 | ✅ | `fangzhengRealized`/`Candidates`/`NoResult` |
| 18 | 小定义方证胜出是分母灌大 | ✅ | 排序器 step 1 惩罚 |
| 19 | 区分呈现/排序语义 | ✅ | 注释明确 |
| 21 | 三个「象」文件层级不同 | ✅ | 症状扁平、脉象层级 |
| 22 | `belongsToLiujing` 是篇章归属 | ✅ | 注释明确 |
| 23 | 八纲判据准入标准 | ✅ | 判据层独立 |
| 24 | 两层分离 | ✅ | 判据层/组合层分离 |
| 25 | 本体缺类先查 | ✅ | 前序遵循 |
| 26 | 本体不一定正确，以医理为准 | ✅ | F2/F3 修复即依此 |
| 27 | 寒热有常虚实无常 | ✅ | 阴阳消解遵循 |
| 28 | Re/Han 硬约束、Xu/Shi 可反转 | ✅ | 本体 axiom 遵循 |
| 30 | 证候组合层落点 | ✅ | `ZhengHouZuhe ⊑ Bingzheng` |
| 31 | 症状层级=投影非替换 | ✅ | 细粒度类保留 |
| 32 | 逻辑链判据→八纲→六经→方证 | ✅ | 单向链路 |
| 34 | 析取⊓析取是性能红线 | ✅ | 未引入 |
| 35 | 六经提纲不足反推病位 | ✅ | 未反推 |
| 36 | 六经八纲医理硬结论 | ✅ | 遵循 |
| 39 | 判据合取项须有 ABox 个体 | ✅ | 前序核查 |
| 40 | 三阶段专用 minibox | ✅ | 已落地 |
| 41 | 合病方证六经只写注解 | ✅ | 未写等价类 |
| 46 | 禁单症状/单脉象⊑八纲 | ⚠️ | 见 4.4 |
| 47 | 按类块整体匹配 | ✅ | 前序修复遵循 |
| 48 | 八纲/六经归属全量审计 | ✅ | 前序已做 |
| 49 | 本体正确性 > 测试通过率 | ✅ | F2/F3 依医书修复 |
| 55 | ABox 不能删只能搬家 | ✅ | 未删 ABox |
| 56 | TBox/ABox 分离落地 | ✅ | import 结构正确 |
| 58 | 名义量不进 TBox | ✅ | `grep hasValue` = **0** |

### 4.3 用户硬约束（★ 43、46、49、51、52、53、57、60）

| 铁律 | 主题 | 判定 | 证据 |
|---|---|---|---|
| ★43 | 八纲只能由症状→判据推出 | ✅ | 1408 行注释「不再用方证兜底反推」 |
| ★46 | 禁单症状/单脉象⊑八纲 | ⚠️ | 见 4.4 |
| ★49 | 本体正确性 > 测试通过率 | ✅ | F2/F3 依医书修复 |
| ★51 | 禁改 OntopOBDAHandler/OpenlletResolver | ✅ | **git status 禁区输出为空** |
| ★52 | 测试目标是正确构建本体 | ✅ | 未为通过测试加反向逻辑 |
| ★53 | 所有改动须有医理医书支持 | ✅ | 生产路径逐条有出处 |
| ★57 | 达标口径（≤240s/≤10s/全过） | ✅ | 20.8s / 1.01s / 279 全过 |
| ★60 | 诊断单向，禁反向推导 | ✅ | 八纲/六经阶段均注释「不再反推」 |

### 4.4 铁律 46 专项核查（唯一存疑项）

铁律 46 原文**明确列出例外**：
> 「既有的 `Dabianying ⊑ Li`、`Fumai ⊑ Biao` 等属**历史遗留，改动需单独评估影响面**。」

`ontology/tcm-maixiang.owl` 现状：

| 公理 | 行 | 判定 |
|---|---|---|
| `Fumai ⊑ Biao`（浮脉⊑表） | 24 | ✅ 铁律 46 明确列为可保留的历史遗留 |
| `Weimai ⊑ Xu`（微脉⊑虚） | 34 | ⚠️ 属病性定义性同义，但**缺依据注释** |
| `Ruomai ⊑ Xu`（弱脉⊑虚） | 35 | ⚠️ 同上 |
| `Shimai ⊑ Shi`（实脉⊑实） | 36 | ⚠️ 同上 |
| `Xumai ⊑ Xu`（虚脉⊑虚） | 37 | ⚠️ 同上（虚脉定义即虚） |
| `Hongmai`（洪脉） | 48 | ✅ **标杆**：已撤 `⊑Yang`，改由 `Panju_B7` 合取判据，注释详列 26/186/25 条 |
| 复合脉 `⊑Xu/⊑Shi/⊑Re/⊑Han`（如 `Fuhuanmai⊑Xu`） | 多处 | ✅ 复合脉本身即合取（浮⊓缓），符合铁律 46 精神 |
| `Xiandamai`（弦大脉） | 509 | ✅ **标杆**：已撤 `⊑Xu/⊑Han`，注释详列濒湖/金匮/268 条/胡希恕 |

**判定**：`Weimai/Ruomai/Shimai/Xumai ⊑ 病性` 属「脉象定义性同义」（虚脉=虚、实脉=实），可归入铁律 46 的「定义性公理」例外，**基本合规**；但与 `Hongmai` 的处理不一致——**建议比照 `Hongmai` 补 `rdfs:comment` 说明保留依据**（如《濒湖脉学》「微脉……气血微兮脉亦微」），使口径统一。

---

## 五、结论

1. **生产路径（真正参与诊断）无主观拍脑袋规则** —— 全部医学判断均有医书/医理出处，逐条可追溯。
2. **非生产路径存在主观参数** —— `JingFangInference`（数据拟合参数）、`SixChannelInferenceEngine`（硬编码脉/舌消歧启发式）。二者**零引用、不在诊断链路**，但建议：
   - 方案 A：删除（推荐，避免误导后人）；
   - 方案 B：类头加 `@Deprecated` + 注释「实验性、非生产、参数无医理依据」。
3. **唯一存疑项**：`Weimai/Ruomai/Shimai/Xumai ⊑ 病性` 缺依据注释（铁律 46），建议比照 `Hongmai` 补注释。
4. **文档失真**：`compareBingweiCoverage` javadoc 悬空引用，建议修正。
5. **60 条铁律**：58 条 ✅ 合规、1 条 ⚠️（铁律 46，缺注释）、1 条 — 不适用（铁律 12/13 等流程类）。**无违规项**。
6. **禁区核验**：`git status --porcelain` 对 `OpenlletResolver`/`OntopOBDAHandler` **输出为空**（铁律 51/59 合规）。

---

*本报告由自动化审计生成，所有证据均可在对应代码行号 / 本体文件 / git 命令中复现。*
