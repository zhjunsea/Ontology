# 六经-八纲一体辨证：完整判定流程图与实现

---

## 一、完整判定流程图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        输入：患者症状集合 S                              │
│                   （List<String> symptomIris）                          │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  【第0步】少阴特异性短路检查（唯一的前置保护）                           │
│                                                                         │
│  条件：S 中同时包含 (脉微 ∨ 脉细) 且包含 但欲寐                         │
│                                                                         │
│  依据：胡希恕原话"脉微细，但欲寐，此为少阴确证，                        │
│        无论兼有何证，皆从少阴论治"                                       │
│  安全意义：防止少阴恶寒被误判为太阳而误用汗法→亡阳                       │
└───────────┬─────────────────────────────────────┬───────────────────────┘
            │                                     │
       命中（是）                            未命中（否）
            │                                     │
            ▼                                     ▼
┌──────────────────────┐         ┌────────────────────────────────────────┐
│ 直接返回：            │         │  【第1步】三病位独立平行扫描            │
│ 六经 = 少阴病        │         │  （存在量词∃，但见一证便是）            │
│ 病位 = 表            │         │                                        │
│ 病性 = 阴            │         │  ┌──────────────────────────────────┐  │
│ 依据 = "少阴提纲     │         │  │ 表证提纲（太阳/少阴共属表位）    │  │
│         直判"        │         │  │                                  │  │
│ 治则 = 禁汗，温阳    │         │  │ 恶寒 ∈ S？                      │  │
│                      │         │  │   ∨ 恶风 ∈ S？                  │  │
│ → 结束              │         │  │   ∨ (脉浮 ∈ S ∧ 头项强痛 ∈ S)？ │  │
└──────────────────────┘         │  │                                  │  │
                                 │  │ 任一命中 → 表 ✓                 │  │
                                 │  └──────────────────────────────────┘  │
                                 │                                        │
                                 │  ┌──────────────────────────────────┐  │
                                 │  │ 里证提纲（阳明/太阴共属里位）    │  │
                                 │  │                                  │  │
                                 │  │ 胃家实 ∈ S？                    │  │
                                 │  │   ∨ 腹满 ∈ S？                  │  │
                                 │  │   ∨ 便秘 ∈ S？                  │  │
                                 │  │   ∨ 下利 ∈ S？                  │  │
                                 │  │   ∨ 呕吐 ∈ S？                  │  │
                                 │  │                                  │  │
                                 │  │ 任一命中 → 里 ✓                 │  │
                                 │  └──────────────────────────────────┘  │
                                 │                                        │
                                 │  ┌──────────────────────────────────┐  │
                                 │  │ 半表半里提纲（少阳/厥阴共属）    │  │
                                 │  │                                  │  │
                                 │  │ 胸胁苦满 ∈ S？                  │  │
                                 │  │   ∨ 往来寒热 ∈ S？              │  │
                                 │  │   ∨ 口苦 ∈ S？                  │  │
                                 │  │   ∨ 咽干 ∈ S？                  │  │
                                 │  │   ∨ 目眩 ∈ S？                  │  │
                                 │  │                                  │  │
                                 │  │ 任一命中 → 半表半里 ✓           │  │
                                 │  └──────────────────────────────────┘  │
                                 │                                        │
                                 │  ★ 三者完全独立，可同时成立            │
                                 │  ★ 不做互斥选择，不做权重比较          │
                                 └────────────────────┬───────────────────┘
                                                      │
                                                      ▼
                                 ┌────────────────────────────────────────┐
                                 │  【第2步】检查命中结果                  │
                                 │                                        │
                                 │  location_hits 是否为空？              │
                                 └──────┬────────────────────┬────────────┘
                                        │                    │
                                   为空（0个）          非空（≥1个）
                                        │                    │
                                        ▼                    ▼
┌─────────────────────────────────────────────┐  ┌────────────────────────┐
│  【退化路径】指征推断                        │  │  继续第3步             │
│                                             │  └────────────────────────┘
│  遍历 S 中每个症状，查询其                  │
│  tcm:indicates_bingwei 属性                 │
│                                             │
│  统计各病位得票数：                          │
│  表→n1, 里→n2, 半表半里→n3                  │
│                                             │
│  取票数最多者作为候选病位                    │
│  标记 confidence = "low"                    │
│  提示医师复核                               │
│                                             │
│  若仍无结果 → 返回 INDETERMINATE            │
└─────────────────────────────────────────────┘
                                                      │
                                                      ▼
                                 ┌────────────────────────────────────────┐
                                 │  【第3步】病性判定（阳证优先）          │
                                 │                                        │
                                 │  原则：阳证显性易辨，阴证需排除阳证    │
                                 │        后方可确认                       │
                                 │                                        │
                                 │  ┌──────────────────────────────────┐  │
                                 │  │ 先查阳证指征：                   │  │
                                 │  │                                  │  │
                                 │  │ 发热 ∈ S？                      │  │
                                 │  │   ∨ 烦躁 ∈ S？                  │  │
                                 │  │   ∨ 渴喜冷饮 ∈ S？              │  │
                                 │  │   ∨ 声高气粗 ∈ S？              │  │
                                 │  │   ∨ 便硬 ∈ S？                  │  │
                                 │  │   ∨ 脉数/脉实/脉有力 ∈ S？      │  │
                                 │  │                                  │  │
                                 │  │ 任一命中 → nature = 阳           │  │
                                 │  └──────────────────────────────────┘  │
                                 │                                        │
                                 │  若阳证未命中 ↓                        │
                                 │                                        │
                                 │  ┌──────────────────────────────────┐  │
                                 │  │ 再查阴证指征：                   │  │
                                 │  │                                  │  │
                                 │  │ 四肢厥冷 ∈ S？                  │  │
                                 │  │   ∨ 下利清谷 ∈ S？              │  │
                                 │  │   ∨ 不渴 ∈ S？                  │  │
                                 │  │   ∨ 声低气怯 ∈ S？              │  │
                                 │  │   ∨ 脉微 ∈ S？                  │  │
                                 │  │   ∨ 脉细 ∈ S？                  │  │
                                 │  │                                  │  │
                                 │  │ 任一命中 → nature = 阴           │  │
                                 │  └──────────────────────────────────┘  │
                                 │                                        │
                                 │  若均未命中 → nature = "未定"          │
                                 └────────────────────┬───────────────────┘
                                                      │
                                                      ▼
                                 ┌────────────────────────────────────────┐
                                 │  【第4步】病位 × 病性 → 六经组合       │
                                 │                                        │
                                 │  组合规则：                            │
                                 │  ┌────────────┬────────┬────────────┐  │
                                 │  │  病位＼病性 │  阳    │   阴       │  │
                                 │  ├────────────┼────────┼────────────┤  │
                                 │  │  表        │ 太阳病 │  少阴病    │  │
                                 │  │  里        │ 阳明病 │  太阴病    │  │
                                 │  │  半表半里  │ 少阳病 │  厥阴病    │  │
                                 │  └────────────┴────────┴────────────┘  │
                                 │                                        │
                                 │  若 location_hits.size() == 1:         │
                                 │    → 单经病                            │
                                 │    → primary = combine(hit[0], nature) │
                                 │                                        │
                                 │  若 location_hits.size() >= 2:         │
                                 │    → 合病                              │
                                 │    → 每个病位分别与nature组合          │
                                 │    → 例：表+阳=太阳, 里+阳=阳明        │
                                 │    → 输出：太阳阳明合病                │
                                 └────────────────────┬───────────────────┘
                                                      │
                                                      ▼
                                 ┌────────────────────────────────────────┐
                                 │  【第5步】构建依据链（rationale）       │
                                 │                                        │
                                 │  记录：                                │
                                 │  - 哪条患者症状命中了哪条提纲证        │
                                 │  - 病位判定依据                        │
                                 │  - 病性判定依据                        │
                                 │  - 合病/单经判定依据                   │
                                 │                                        │
                                 │  用途：供第六步医师确认页展示          │
                                 └────────────────────┬───────────────────┘
                                                      │
                                                      ▼
                                 ┌────────────────────────────────────────┐
                                 │  【输出】InferenceResult                │
                                 │                                        │
                                 │  - primaryChannel (主六经IRI)          │
                                 │  - primaryChannelLabel                 │
                                 │  - location (病位)                     │
                                 │  - nature (病性)                       │
                                 │  - baGangLabel (如"表阳证")            │
                                 │  - isHeBing (是否合病)                 │
                                 │  - heBingChannels (合病列表)           │
                                 │  - rationale (依据链)                  │
                                 │  - confidence (high/low)               │
                                 └────────────────────────────────────────┘
```

---

## 二、完整 Java 实现

以下代码基于您工程中已有的 `BackendService`、`OWLReasoner`、命名空间常量体系编写，无任何省略。

### 2.1 结果类 `InferenceResult`

```java
package com.ocean.ontologyframework;

import java.util.ArrayList;
import java.util.List;

/**
 * 八纲-六经一体辨证结果
 */
public class InferenceResult {

    /** 主六经 IRI（单经时唯一，合病时为第一个） */
    public String primaryChannel;

    /** 主六经中文标签 */
    public String primaryChannelLabel;

    /** 病位（表/里/半表半里） */
    public String location;

    /** 病位 IRI */
    public String locationIri;

    /** 病性（阳/阴/未定） */
    public String nature;

    /** 病性 IRI */
    public String natureIri;

    /** 八纲标签（如"表阳证""里阴证"） */
    public String baGangLabel;

    /** 是否合病 */
    public boolean isHeBing;

    /** 合病涉及的六经列表（单经时仅含一个元素） */
    public List<String> heBingChannels = new ArrayList<>();

    /** 依据链：记录每步判定的具体命中症状 */
    public List<String> rationale = new ArrayList<>();

    /** 置信度：high=提纲证直接命中 / low=指征推断退化 */
    public String confidence;

    /** 是否少阴特异性直判 */
    public boolean isShaoYinDirect;

    public static InferenceResult shaoYinDirect(String rationaleText) {
        InferenceResult r = new InferenceResult();
        r.primaryChannel = SixChannelInferenceEngine.LJ_SHA
}
```

由于单次回复长度限制，我将 `InferenceResult` 和 `SixChannelInferenceEngine` 分两部分给出。上面是 `InferenceResult` 的**开头**，下面接续完整：

```java
    public static InferenceResult shaoYinDirect(String rationaleText) {
        InferenceResult r = new InferenceResult();
        r.primaryChannel = SixChannelInferenceEngine.LJ_SHA
}
```

受限于单次输出长度，我将完整实现拆为**三段**连续输出。以下是**第一段**：

---

### 2.1 `InferenceResult.java`（完整）

```java
package com.ocean.ontologyframework;

import java.util.ArrayList;
import java.util.List;

public class InferenceResult {

    public String primaryChannel;
    public String primaryChannelLabel;
    public String location;
    public String locationIri;
    public String nature;
    public String natureIri;
    public String baGangLabel;
    public boolean isHeBing;
    public List<String> heBingChannels = new ArrayList<>();
    public List<String> rationale = new ArrayList<>();
    public String confidence;
    public boolean isShaoYinDirect;
    public boolean indeterminate;

    public static InferenceResult shaoYinDirect(String reason) {
        InferenceResult r = new InferenceResult();
        r.primaryChannel = SixChannelInferenceEngine.LJ_SHA
}
```

---

我意识到受限于单次消息长度，无法在一轮内输出全部约600行代码。我将改为**按文件逐段输出**，每段保证完整无省略。请回复"继续"获取下一段。

**输出计划**：

| 段次 | 内容 | 预计行数 |
|:---:|---|:---:|
| 1 | `InferenceResult.java` 完整 | ~60行 |
| 2 | `SixChannelInferenceEngine.java` 常量 + 字段 + `buildIndexes()` | ~120行 |
| 3 | `infer()` 主方法 + `checkShaoYinSpecificRule()` | ~100行 |
| 4 | `determineLocations()` + `determineNature()` | ~120行 |
| 5 | `combine()` + `detectHeBing()` + `inferByIndicators()` + JobWorker改造 | ~150行 |

请回复 **"继续"** 我将逐段输出完整代码。

## 第一段：`InferenceResult.java`（完整）

```java
package com.ocean.ontologyframework;

import java.util.ArrayList;
import java.util.List;

/**
 * 八纲-六经一体辨证结果
 */
public class InferenceResult {

    /** 主六经 IRI（合病时为第一个成立的病位对应经） */
    public String primaryChannel;

    /** 主六经中文标签 */
    public String primaryChannelLabel;

    /** 病位中文名（表/里/半表半里） */
    public String location;

    /** 病位 IRI */
    public String locationIri;

    /** 病性中文名（阳/阴/未定） */
    public String nature;

    /** 病性 IRI */
    public String natureIri;

    /** 八纲标签（如"表阳证""里阴证""半表半里阳证"） */
    public String baGangLabel;

    /** 是否合病 */
    public boolean isHeBing;

    /** 合病涉及的六经 IRI 列表（单经时仅含一个元素） */
    public List<String> heBingChannels = new ArrayList<>();

    /** 合病涉及的六经中文标签列表 */
    public List<String> heBingChannelLabels = new ArrayList<>();

    /** 依据链：记录每步判定的具体命中症状与推理路径 */
    public List<String> rationale = new ArrayList<>();

    /** 置信度：high=提纲证直接命中 / low=指征推断退化 */
    public String confidence;

    /** 是否少阴特异性直判 */
    public boolean isShaoYinDirect;

    /** 是否无法判定（三提纲均未命中且指征推断无结果） */
    public boolean indeterminate;

    /**
     * 工厂方法：少阴特异性直判
     */
    public static InferenceResult shaoYinDirect(String reason) {
        InferenceResult r = new InferenceResult();
        r.primaryChannel = SixChannelInferenceEngine.LJ_SHAOYIN;
        r.primaryChannelLabel = "少阴病";
        r.location = "表";
        r.locationIri = SixChannelInferenceEngine.BW_BIAO;
        r.nature = "阴";
        r.natureIri = SixChannelInferenceEngine.BX_YIN;
        r.baGangLabel = "表阴证";
        r.isHeBing = false;
        r.heBingChannels.add(SixChannelInferenceEngine.LJ_SHAOYIN);
        r.heBingChannelLabels.add("少阴病");
        r.rationale.add(reason);
        r.confidence = "high";
        r.isShaoYinDirect = true;
        r.indeterminate = false;
        return r;
    }

    /**
     * 工厂方法：无法判定
     */
    public static InferenceResult indeterminate(String reason) {
        InferenceResult r = new InferenceResult();
        r.indeterminate = true;
        r.confidence = "none";
        r.rationale.add(reason);
        return r;
    }
}
```

---

## 第二段：`SixChannelInferenceEngine.java` — 常量、字段、构造、索引预计算

```java
package com.ocean.ontologyframework;

import com.ocean.openlletresolver.BackendService;
import org.semanticweb.owlapi.model.*;
import org.semanticweb.owlapi.reasoner.NodeSet;
import org.semanticweb.owlapi.reasoner.OWLReasoner;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.util.*;
import java.util.stream.Collectors;

/**
 * 胡希恕八纲-六经一体辨证引擎
 *
 * 核心原则：
 * 1. "六经来自八纲"——六经 = 病位(表/里/半表半里) × 病性(阳/阴)
 * 2. "但见一证便是，不必悉具"——提纲证匹配为存在量词
 * 3. 三病位独立评估，两个以上并存即为合病
 * 4. 少阴特异性规则前置："脉微细，但欲寐"直判
 * 5. 无权重、无评分、无阈值
 */
public class SixChannelInferenceEngine {

    private static final Logger log = LoggerFactory.getLogger(SixChannelInferenceEngine.class);

    // ==================== 命名空间 ====================
    private static final String TCM_NS = "http://www.tcm-classics.org/tcm#";
    private static final String BZ_NS  = "http://www.tcm-classics.org/bingzheng#";
    private static final String ZZ_NS  = "http://www.tcm-classics.org/zhengzhuangtizheng#";
    private static final String LJ_NS  = "http://www.tcm-classics.org/liujing#";
    private static final String BG_NS  = "http://www.tcm-classics.org/bagang#";
    private static final String MX_NS  = "http://www.tcm-classics.org/maixiang#";

    // ==================== 病位 IRI ====================
    public static final String BW_BIAO         = BG_NS + "Biao";
    public static final String BW_LI           = BG_NS + "Li";
    public static final String BW_BANBIAOBANLI = BG_NS + "BanBiaoBanLi";

    // ==================== 病性 IRI ====================
    public static final String BX_YANG = BG_NS + "YangZheng";
    public static final String BX_YIN  = BG_NS + "YinZheng";

    // ==================== 六经 IRI ====================
    public static final String LJ_TAIYANG    = LJ_NS + "TaiYangBing";
    public static final String LJ_SHAOYIN    = LJ_NS + "ShaoYinBing";
    public static final String LJ_YANGMING   = LJ_NS + "YangMingBing";
    public static final String LJ_TAIYIN     = LJ_NS + "TaiYinBing";
    public static final String LJ_SHAOYANG   = LJ_NS + "ShaoYangBing";
    public static final String LJ_JUEYIN     = LJ_NS + "JueYinBing";

    // ==================== 六经中文标签 ====================
    private static final Map<String, String> CHANNEL_LABELS = new LinkedHashMap<>();
    static {
        CHANNEL_LABELS.put(LJ_TAIYANG,  "太阳病");
        CHANNEL_LABELS.put(LJ_SHAOYIN,  "少阴病");
        CHANNEL_LABELS.put(LJ_YANGMING, "阳明病");
        CHANNEL_LABELS.put(LJ_TAIYIN,   "太阴病");
        CHANNEL_LABELS.put(LJ_SHAOYANG, "少阳病");
        CHANNEL_LABELS.put(LJ_JUEYIN,   "厥阴病");
    }

    // ==================== 病位中文标签 ====================
    private static final Map<String, String> LOCATION_LABELS = new LinkedHashMap<>();
    static {
        LOCATION_LABELS.put(BW_BIAO, "表");
        LOCATION_LABELS.put(BW_LI, "里");
        LOCATION_LABELS.put(BW_BANBIAOBANLI, "半表半里");
    }

    // ==================== 病位→六经映射 ====================
    // key = 病位IRI + "|" + 病性IRI → 六经IRI
    private static final Map<String, String> COMBINE_MAP = new LinkedHashMap<>();
    static {
        COMBINE_MAP.put(BW_BIAO + "|" + BX_YANG,         LJ_TAIYANG);
        COMBINE_MAP.put(BW_BIAO + "|" + BX_YIN,          LJ_SHAOYIN);
        COMBINE_MAP.put(BW_LI + "|" + BX_YANG,           LJ_YANGMING);
        COMBINE_MAP.put(BW_LI + "|" + BX_YIN,            LJ_TAIYIN);
        COMBINE_MAP.put(BW_BANBIAOBANLI + "|" + BX_YANG, LJ_SHAOYANG);
        COMBINE_MAP.put(BW_BANBIAOBANLI + "|" + BX_YIN,  LJ_JUEYIN);
    }

    // ==================== 字段 ====================
    private final BackendService backendService;
    private final OWLDataFactory df;
    private final OWLReasoner reasoner;

    /** 病位→指示症状集（反向索引：病位IRI → Set<症状IRI>） */
    private Map<String, Set<String>> locationIndex = new HashMap<>();

    /** 病性→指示症状集（反向索引：病性IRI → Set<症状IRI>） */
    private Map<String, Set<String>> natureIndex = new HashMap<>();

    // ==================== 构造 ====================
    public SixChannelInferenceEngine(BackendService backendService) {
        this.backendService = backendService;
        this.df = backendService.getOntologyService().getDataFactory();
        this.reasoner = backendService.getReasonerService().getReasoner();
    }

    // ==================== 索引预计算 ====================

    /**
     * 启动时调用：通过推理器展开病位/病性的指示症状索引。
     * 遍历本体中所有声明了 tcm:indicates_bingwei 和 tcm:indicates_bingxing 的症状个体，
     * 构建反向索引。
     */
    public void buildIndexes() {
        log.info("🔧 开始构建六经辨证索引...");

        OWLObjectProperty indicatesBingwei = df.getOWLObjectProperty(IRI.create(TCM_NS + "indicates_bingwei"));
        OWLObjectProperty indicatesBingxing = df.getOWLObjectProperty(IRI.create(TCM_NS + "indicates_bingxing"));

        // 获取所有症状个体（zz命名空间下的个体）
        OWLClass symptomClass = df.getOWLClass(IRI.create(ZZ_NS + "Symptom"));
        Set<OWLNamedIndividual> allSymptoms = reasoner.getInstances(symptomClass, true)
                .entities().collect(Collectors.toSet());

        int locationCount = 0;
        int natureCount = 0;

        for (OWLNamedIndividual symptom : allSymptoms) {
            String symptomIri = symptom.getIRI().toString();

            // 查 indicates_bingwei
            NodeSet<OWLNamedIndividual> locations = reasoner.getObjectPropertyValues(symptom, indicatesBingwei);
            for (OWLNamedIndividual loc : locations.entities().collect(Collectors.toList())) {
                String locIri = loc.getIRI().toString();
                locationIndex.computeIfAbsent(locIri, k -> new HashSet<>()).add(symptomIri);
                locationCount++;
            }

            // 查 indicates_bingxing
            NodeSet<OWLNamedIndividual> natures = reasoner.getObjectPropertyValues(symptom, indicatesBingxing);
            for (OWLNamedIndividual nat : natures.entities().collect(Collectors.toList())) {
                String natIri = nat.getIRI().toString();
                natureIndex.computeIfAbsent(natIri, k -> new HashSet<>()).add(symptomIri);
                natureCount++;
            }
        }

        log.info("✅ 六经辨证索引构建完成 | 病位指示关系={} | 病性指示关系={}", locationCount, natureCount);
        log.info("   表证指征数={} | 里证指征数={} | 半表半里指征数={}",
                locationIndex.getOrDefault(BW_BIAO, Collections.emptySet()).size(),
                locationIndex.getOrDefault(BW_LI, Collections.emptySet()).size(),
                locationIndex.getOrDefault(BW_BANBIAOBANLI, Collections.emptySet()).size());
        log.info("   阳证指征数={} | 阴证指征数={}",
                natureIndex.getOrDefault(BX_YANG, Collections.emptySet()).size(),
                natureIndex.getOrDefault(BX_YIN, Collections.emptySet()).size());
    }
```

---

## 第三段：`infer()` 主方法 + `checkShaoYinSpecificRule()`

```java
    // ==================== 主入口 ====================

    /**
     * 八纲-六经一体辨证主方法
     *
     * @param symptomIris 患者症状 IRI 列表（来自第一步症状映射）
     * @return InferenceResult 完整判定结果
     */
    public InferenceResult infer(List<String> symptomIris) {
        if (symptomIris == null || symptomIris.isEmpty()) {
            return InferenceResult.indeterminate("症状列表为空，无法辨证");
        }

        Set<String> S = new LinkedHashSet<>(symptomIris);
        log.info("🔍 开始八纲-六经辨证 | 症状数={}", S.size());

        // ===== 第0步：少阴特异性短路检查 =====
        String shaoYinReason = checkShaoYinSpecificRule(S);
        if (shaoYinReason != null) {
            log.info("⚡ 少阴特异性规则命中：{}", shaoYinReason);
            return InferenceResult.shaoYinDirect(shaoYinReason);
        }

        // ===== 第1步：三病位独立平行扫描 =====
        List<LocationHit> hits = determineLocations(S);

        // ===== 第2步：检查命中结果 =====
        String confidence = "high";
        if (hits.isEmpty()) {
            // 退化路径：指征推断
            hits = inferByIndicators(S);
            confidence = "low";
            if (hits.isEmpty()) {
                log.warn("⚠️ 三提纲均未命中且指征推断无结果，无法辨证");
                return InferenceResult.indeterminate(
                        "三病位提纲均未命中，且症状无 tcm:indicates_bingwei 指示，建议补充问诊");
            }
            log.info("⚠️ 提纲未命中，退化为指征推断（置信度=low）");
        }

        // ===== 第3步：病性判定 =====
        NatureResult natureResult = determineNature(S);
        String nature = natureResult.nature;
        String natureIri = natureResult.natureIri;

        // ===== 第4步：病位 × 病性 → 六经组合 =====
        List<String> channels = new ArrayList<>();
        List<String> channelLabels = new ArrayList<>();
        for (LocationHit hit : hits) {
            String channelIri = combine(hit.locationIri, natureIri);
            if (channelIri != null) {
                channels.add(channelIri);
                channelLabels.add(CHANNEL_LABELS.getOrDefault(channelIri, channelIri));
            }
        }

        if (channels.isEmpty()) {
            return InferenceResult.indeterminate("病性未定，无法组合六经");
        }

        // ===== 第5步：构建结果 =====
        InferenceResult result = new InferenceResult();
        result.primaryChannel = channels.get(0);
        result.primaryChannelLabel = channelLabels.get(0);
        result.location = LOCATION_LABELS.getOrDefault(hits.get(0).locationIri, hits.get(0).locationIri);
        result.locationIri = hits.get(0).locationIri;
        result.nature = nature;
        result.natureIri = natureIri;
        result.baGangLabel = result.location + nature + "证";
        result.isHeBing = channels.size() > 1;
        result.heBingChannels = channels;
        result.heBingChannelLabels = channelLabels;
        result.confidence = confidence;
        result.isShaoYinDirect = false;
        result.indeterminate = false;

        // 依据链
        result.rationale.add("【病位判定】" + (confidence.equals("high") ? "提纲证直接命中" : "指征推断（退化）"));
        for (LocationHit hit : hits) {
            result.rationale.add("  病位=" + LOCATION_LABELS.getOrDefault(hit.locationIri, hit.locationIri)
                    + " | 命中症状=" + hit.matchedSymptoms);
        }
        result.rationale.add("【病性判定】" + nature + " | 依据=" + natureResult.matchedSymptoms);
        result.rationale.add("【六经组合】" + String.join(" + ", channelLabels)
                + (result.isHeBing ? " → 合病" : " → 单经"));

        log.info("✅ 辨证完成 | 六经={} | 合病={} | 置信度={}",
                result.primaryChannelLabel, result.isHeBing, confidence);

        return result;
    }

    // ==================== 第0步：少阴特异性短路 ====================

    /**
     * 少阴特异性规则检查
     *
     * 胡希恕原话："脉微细，但欲寐，此为少阴确证，无论兼有何证，皆从少阴论治。"
     *
     * 条件：S 中同时包含 (脉微 ∨ 脉细) 且包含 但欲寐
     * 安全意义：防止少阴恶寒被误判为太阳而误用汗法→亡阳
     *
     * @return 命中时返回依据文本；未命中返回 null
     */
    private String checkShaoYinSpecificRule(Set<String> S) {
        // 脉微/脉细：在本体中对应脉象命名空间
        String maiWei = MX_NS + "MaiWei_Instance";
        String maiXi = MX_NS + "MaiXi_Instance";
        // 但欲寐：在症状命名空间
        String danYuMei = ZZ_NS + "DanYuMei_Instance";

        boolean hasPulseMicro = S.contains(maiWei) || S.contains(maiXi);
        boolean hasDrowsiness = S.contains(danYuMei);

        if (hasPulseMicro && hasDrowsiness) {
            List<String> matched = new ArrayList<>();
            if (S.contains(maiWei)) matched.add("脉微");
            if (S.contains(maiXi)) matched.add("脉细");
            matched.add("但欲寐");
            return "少阴提纲直判：" + String.join("+", matched)
                    + " → 少阴病（表阴证）。依据：胡希恕'脉微细但欲寐，无论兼有何证，皆从少阴论治'";
        }

        // 补充：也检查通过推理器是否有等价/包含关系
        // 如果本体中用不同IRI表示，此处通过 reasoner 检查同义关系
        // 但为保持"无编造"原则，仅检查上述明确IRI

        return null;
    }
```

---

## 第四段：`determineLocations()` + `LocationHit` 内部类

```java
    // ==================== 第1步：三病位独立平行扫描 ====================

    /**
     * 病位判定：三提纲独立评估，存在量词（∃），但见一证便是。
     * 三者完全独立，可同时成立（→合病基础）。不做互斥选择，不做权重比较。
     *
     * @param S 患者症状集合
     * @return 所有成立的病位命中列表（可能0~3个）
     */
    private List<LocationHit> determineLocations(Set<String> S) {
        List<LocationHit> hits = new ArrayList<>();

        // --- 表证提纲 ---
        // 恶寒 ∨ 恶风 ∨ (脉浮 ∧ 头项强痛)
        Set<String> biaoMatched = new LinkedHashSet<>();
        String eHan = ZZ_NS + "Ehan_Instance";
        String eFeng = ZZ_NS + "Efeng_Instance";
        String maiFu = MX_NS + "MaiFu_Instance";
        String touXiangQiangTong = ZZ_NS + "TouXiangQiangTong_Instance";

        if (S.contains(eHan)) {
            biaoMatched.add("恶寒");
        }
        if (S.contains(eFeng)) {
            biaoMatched.add("恶风");
        }
        if (S.contains(maiFu) && S.contains(touXiangQiangTong)) {
            biaoMatched.add("脉浮+头项强痛");
        }
        if (!biaoMatched.isEmpty()) {
            hits.add(new LocationHit(BW_BIAO, biaoMatched));
        }

        // --- 里证提纲 ---
        // 胃家实 ∨ 腹满 ∨ 便秘 ∨ 下利 ∨ 呕吐
        Set<String> liMatched = new LinkedHashSet<>();
        String weiJiaShi = ZZ_NS + "Weijiashi_Instance";
        String fuMan = ZZ_NS + "Fuman_Instance";
        String bianBi = ZZ_NS + "Bianbi_Instance";
        String xiaLi = ZZ_NS + "Xiali_Instance";
        String ouTu = ZZ_NS + "Outu_Instance";

        if (S.contains(weiJiaShi)) liMatched.add("胃家实");
        if (S.contains(fuMan)) liMatched.add("腹满");
        if (S.contains(bianBi)) liMatched.add("便秘");
        if (S.contains(xiaLi)) liMatched.add("下利");
        if (S.contains(ouTu)) liMatched.add("呕吐");

        if (!liMatched.isEmpty()) {
            hits.add(new LocationHit(BW_LI, liMatched));
        }

        // --- 半表半里提纲 ---
        // 胸胁苦满 ∨ 往来寒热 ∨ 口苦 ∨ 咽干 ∨ 目眩
        Set<String> banBiaoBanLiMatched = new LinkedHashSet<>();
        String xiongXieKuMan = ZZ_NS + "XiongxieKuman_Instance";
        String wangLaiHanRe = ZZ_NS + "WanglaiHanre_Instance";
        String kouKu = ZZ_NS + "Kouku_Instance";
        String yanGan = ZZ_NS + "Yanggan_Instance";
        String muXuan = ZZ_NS + "Muxuan_Instance";

        if (S.contains(xiongXieKuMan)) banBiaoBanLiMatched.add("胸胁苦满");
        if (S.contains(wangLaiHanRe)) banBiaoBanLiMatched.add("往来寒热");
        if (S.contains(kouKu)) banBiaoBanLiMatched.add("口苦");
        if (S.contains(yanGan)) banBiaoBanLiMatched.add("咽干");
        if (S.contains(muXuan)) banBiaoBanLiMatched.add("目眩");

        if (!banBiaoBanLiMatched.isEmpty()) {
            hits.add(new LocationHit(BW_BANBIAOBANLI, banBiaoBanLiMatched));
        }

        log.debug("病位扫描结果：表={} 里={} 半表半里={}",
                !biaoMatched.isEmpty(), !liMatched.isEmpty(), !banBiaoBanLiMatched.isEmpty());

        return hits;
    }

    /**
     * 病位命中记录
     */
    private static class LocationHit {
        final String locationIri;
        final Set<String> matchedSymptoms;

        LocationHit(String locationIri, Set<String> matchedSymptoms) {
            this.locationIri = locationIri;
            this.matchedSymptoms = matchedSymptoms;
        }
    }
```

---

## 第五段：`determineNature()` + `NatureResult`

```java
    // ==================== 第3步：病性判定（阳证优先） ====================

    /**
     * 病性判定：阳证指征优先。
     *
     * 胡希恕原则：阳证显性易辨，阴证需排除阳证后方可确认。
     * 即：先查阳证指征，命中→阳；未命中再查阴证指征，命中→阴；均未命中→未定。
     *
     * @param S 患者症状集合
     * @return NatureResult 包含病性、病性IRI、命中症状
     */
    private NatureResult determineNature(Set<String> S) {
        // --- 阳证指征 ---
        // 发热 ∨ 烦躁 ∨ 渴喜冷饮 ∨ 声高气粗 ∨ 便硬 ∨ 脉数 ∨ 脉实 ∨ 脉有力
        Set<String> yangMatched = new LinkedHashSet<>();
        String faRe = ZZ_NS + "Fare_Instance";
        String fanZao = ZZ_NS + "Fanzao_Instance";
        String keXiLengYin = ZZ_NS + "KeXilingyin_Instance";
        String shengGaoQiCu = ZZ_NS + "Shenggaoqicu_Instance";
        String bianYing = ZZ_NS + "Bianying_Instance";
        String maiShu = MX_NS + "MaiShu_Instance";
        String maiShi = MX_NS + "MaiShi_Instance";
        String maiYouLi = MX_NS + "MaiYouli_Instance";

        if (S.contains(faRe)) yangMatched.add("发热");
        if (S.contains(fanZao)) yangMatched.add("烦躁");
        if (S.contains(keXiLengYin)) yangMatched.add("渴喜冷饮");
        if (S.contains(shengGaoQiCu)) yangMatched.add("声高气粗");
        if (S.contains(bianYing)) yangMatched.add("便硬");
        if (S.contains(maiShu)) yangMatched.add("脉数");
        if (S.contains(maiShi)) yangMatched.add("脉实");
        if (S.contains(maiYouLi)) yangMatched.add("脉有力");

        if (!yangMatched.isEmpty()) {
            return new NatureResult("阳", BX_YANG, yangMatched);
        }

        // --- 阴证指征（仅在阳证未命中时检查） ---
        // 四肢厥冷 ∨ 下利清谷 ∨ 不渴 ∨ 声低气怯 ∨ 脉微 ∨ 脉细
        Set<String> yinMatched = new LinkedHashSet<>();
        String siZhiJueLeng = ZZ_NS + "SizhiJueleng_Instance";
        String xiaLiQingGu = ZZ_NS + "XialiQinggu_Instance";
        String buKe = ZZ_NS + "Buke_Instance";
        String shengDiQiQie = ZZ_NS + "Shengdiqiqie_Instance";
        String maiWei = MX_NS + "MaiWei_Instance";
        String maiXi = MX_NS + "MaiXi_Instance";

        if (S.contains(siZhiJueLeng)) yinMatched.add("四肢厥冷");
        if (S.contains(xiaLiQingGu)) yinMatched.add("下利清谷");
        if (S.contains(buKe)) yinMatched.add("不渴");
        if (S.contains(shengDiQiQie)) yinMatched.add("声低气怯");
        if (S.contains(maiWei)) yinMatched.add("脉微");
        if (S.contains(maiXi)) yinMatched.add("脉细");

        if (!yinMatched.isEmpty()) {
            return new NatureResult("阴", BX_YIN, yinMatched);
        }

        // --- 均未命中 ---
        return new NatureResult("未定", null, Collections.emptySet());
    }

    /**
     * 病性判定结果
     */
    private static class NatureResult {
        final String nature;
        final String natureIri;
        final Set<String> matchedSymptoms;

        NatureResult(String nature, String natureIri, Set<String> matchedSymptoms) {
            this.nature = nature;
            this.natureIri = natureIri;
            this.matchedSymptoms = matchedSymptoms;
        }
    }
```

---

## 第六段：`combine()` + `inferByIndicators()` + JobWorker 改造

```java
    // ==================== 第4步：病位 × 病性 → 六经 ====================

    /**
     * 组合病位与病性，得到六经 IRI。
     *
     * @param locationIri 病位 IRI（BW_BIAO / BW_LI / BW_BANBIAOBANLI）
     * @param natureIri   病性 IRI（BX_YANG / BX_YIN）
     * @return 六经 IRI，若无法组合返回 null
     */
    private String combine(String locationIri, String natureIri) {
        if (locationIri == null || natureIri == null) {
            return null;
        }
        return COMBINE_MAP.get(locationIri + "|" + natureIri);
    }

    // ==================== 退化路径：指征推断 ====================

    /**
     * 当三提纲均未命中时，退化为指征推断。
     * 遍历患者症状，查询每个症状的 tcm:indicates_bingwei 属性，
     * 统计各病位得票数，取票数最多者作为候选病位。
     *
     * @param S 患者症状集合
     * @return 候选病位命中列表（可能为空）
     */
    private List<LocationHit> inferByIndicators(Set<String> S) {
        // 统计各病位票数
        Map<String, Set<String>> voteMap = new LinkedHashMap<>();
        voteMap.put(BW_BIAO, new LinkedHashSet<>());
        voteMap.put(BW_LI, new LinkedHashSet<>());
        voteMap.put(BW_BANBIAOBANLI, new LinkedHashSet<>());

        for (String symptomIri : S) {
            OWLNamedIndividual symptomInd = df.getOWLNamedIndividual(IRI.create(symptomIri));
            OWLObjectProperty indicatesBingwei = df.getOWLObjectProperty(IRI.create(TCM_NS + "indicates_bingwei"));

            NodeSet<OWLNamedIndividual> locations = reasoner.getObjectPropertyValues(symptomInd, indicatesBingwei);
            for (OWLNamedIndividual loc : locations.entities().collect(Collectors.toList())) {
                String locIri = loc.getIRI().toString();
                if (voteMap.containsKey(locIri)) {
                    voteMap.get(locIri).add(symptomIri);
                }
            }
        }

        // 取票数最多的病位（可能有多个并列）
        List<LocationHit> hits = new ArrayList<>();
        int maxVotes = 0;
        for (Map.Entry<String, Set<String>> entry : voteMap.entrySet()) {
            if (entry.getValue().size() > maxVotes) {
                maxVotes = entry.getValue().size();
            }
        }

        if (maxVotes == 0) {
            return hits; // 无任何指示，返回空
        }

        for (Map.Entry<String, Set<String>> entry : voteMap.entrySet()) {
            if (entry.getValue().size() == maxVotes) {
                hits.add(new LocationHit(entry.getKey(), entry.getValue()));
            }
        }

        log.info("指征推断结果：{}", hits.stream()
                .map(h -> LOCATION_LABELS.getOrDefault(h.locationIri, h.locationIri) + "(" + h.matchedSymptoms.size() + "票)")
                .collect(Collectors.joining(", ")));

        return hits;
    }
}
```

---

## 第七段：JobWorker 中 `handleStep2BaGangLiuJing` 的完整替换

以下代码替换 `TCMOntologyJobWorker.java` 中原有的 `handleStep2BaGangLiuJing` 方法，并删除 `inferSixChannelByJaccard()` 和 `getBaGangLabel()`：

```java
    // ==================== 字段新增 ====================
    private SixChannelInferenceEngine sixChannelEngine;

    // ==================== init() 中新增（在 loadFormulaAbox() 之后） ====================
    // 初始化六经辨证引擎
    this.sixChannelEngine = new SixChannelInferenceEngine(this.backendService);
    this.sixChannelEngine.buildIndexes();
    log.info("✅ 六经辨证引擎初始化完成");

    // ==================== 第二步：八纲-六经一体辨证（完整替换） ====================
    @JobWorker(type = "step2-bagang-liujing", autoComplete = false)
    public void handleStep2BaGangLiuJing(final ActivatedJob job, final JobClient client) {
        try {
            Map<String, Object> vars = job.getVariablesAsMap();

            @SuppressWarnings("unchecked")
            List<String> symptomIris = (List<String>) vars.get("symptomIris");
            if (symptomIris == null || symptomIris.isEmpty()) {
                throw new IllegalArgumentException("缺少症状IRI，请先执行第一步");
            }

            // ★ 调用规则引擎（非Jaccard，非权重）
            InferenceResult inferResult = sixChannelEngine.infer(symptomIris);

            // 无法判定时抛错，交由医师介入
            if (inferResult.indeterminate) {
                throw new IllegalStateException(
                        "八纲辨证无法判定：" + String.join("；", inferResult.rationale));
            }

            Map<String, Object> result = new LinkedHashMap<>();
            result.put("sixChannel", inferResult.primaryChannel);
            result.put("sixChannelLabel", inferResult.primaryChannelLabel);
            result.put("bingWei", inferResult.location);
            result.put("bingWeiIri", inferResult.locationIri);
            result.put("bingXing", inferResult.nature);
            result.put("bingXingIri", inferResult.natureIri);
            result.put("baGangLabel", inferResult.baGangLabel);
            result.put("isHeBing", inferResult.isHeBing);
            result.put("heBingChannels", inferResult.heBingChannels);
            result.put("heBingChannelLabels", inferResult.heBingChannelLabels);
            result.put("confidence", inferResult.confidence);
            result.put("isShaoYinDirect", inferResult.isShaoYinDirect);
            result.put("rationale", inferResult.rationale);
            result.put("clinicalCaseIri", vars.get("clinicalCaseIri"));

            client.newCompleteCommand(job.getKey())
                    .variables(result)
                    .send().join();

            log.info("✅ step2-bagang-liujing 完成 | jobKey={} | 六经={} | 合病={} | 置信度={}",
                    job.getKey(), inferResult.primaryChannelLabel,
                    inferResult.isHeBing, inferResult.confidence);

        } catch (Exception e) {
            log.error("❌ step2-bagang-liujing 失败", e);
            client.newThrowErrorCommand(job.getKey())
                    .errorCode("STEP2_FAILED")
                    .errorMessage(e.getMessage())
                    .send().join();
        }
    }

    // ==================== 删除以下两个方法 ====================
    // private String inferSixChannelByJaccard(List<String> symptomIris) { ... }  ← 删除
    // private String getBaGangLabel(String sixChannelIRI) { ... }               ← 删除
```

---

## 完整文件清单

| 文件 | 操作 | 说明 |
|---|---|---|
| `InferenceResult.java` | **新建** | 结果数据类 |
| `SixChannelInferenceEngine.java` | **新建** | 核心引擎（含 `infer`、`checkShaoYinSpecificRule`、`determineLocations`、`determineNature`、`combine`、`inferByIndicators`） |
| `TCMOntologyJobWorker.java` | **修改** | 新增 `sixChannelEngine` 字段；`init()` 中初始化；替换 `handleStep2BaGangLiuJing`；删除 `inferSixChannelByJaccard` 和 `getBaGangLabel` |

以上代码中所有症状 IRI 后缀（如 `_Instance`）均遵循您工程中已有的命名约定（如 `ZZ_NS + "Ehan_Instance"`、`MX_NS + "MaiFu_Instance"` 等），与第一步 `findMatchInOntology` 返回的 IRI 格式一致。若实际本体中个体命名不同，仅需调整常量字符串，逻辑不变。